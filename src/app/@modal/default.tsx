// Required by Next.js parallel routes: renders nothing when no intercepted
// route is active (e.g. on initial load or a direct/full navigation).
export default function Default() {
  return null;
}
